<UserControl x:Class="Dzl.Tray.Views.MyModsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:Dzl.Tray"
             xmlns:vm="clr-namespace:Dzl.Tray.ViewModels"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:MainViewModel}">
  <!-- My Mods page (source projects): create / import / clone-from-GitHub, then per-project
       Build / Git / Open. DataContext = MainViewModel (inherited from the host window). -->
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <StackPanel Grid.Row="0" Margin="0,0,0,12">
      <Grid>
        <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" Text="My Mods" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center" />
        <ui:Button Grid.Column="1" Icon="{ui:SymbolIcon Settings24}" ToolTip="Mods settings"
                   Appearance="Secondary" Padding="8,4" Tag="mods" Click="OnModuleSettings" />
      </Grid>
      <TextBlock FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}">
        <Run Text="Source projects under " /><Run Text="{Binding ProjectsRoot, Mode=OneWay}" FontWeight="SemiBold" />
      </TextBlock>
      <!-- P: status — building needs the work drive mounted -->
      <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
        <Ellipse Width="9" Height="9" VerticalAlignment="Center" Margin="0,0,8,0"
                 Fill="{Binding WorkDriveMounted, Converter={StaticResource StatusDotConverter}}" />
        <TextBlock Text="{Binding WorkDriveStatus}" VerticalAlignment="Center" FontSize="12"
                   Foreground="{DynamicResource TextFillColorTertiaryBrush}" Margin="0,0,8,0" />
        <ui:Button Content="Mount P:" Padding="8,2" FontSize="12" Command="{Binding MountWorkDriveCommand}"
                   Visibility="{Binding WorkDriveMounted, Converter={StaticResource InverseBoolToVis}}" />
      </StackPanel>
    </StackPanel>

    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
      <StackPanel Margin="0,0,14,0">

        <!-- Warning: P: not mounted → building is blocked -->
        <Border Margin="0,0,0,12" Padding="12" CornerRadius="6"
                Background="#3A2A0F" BorderBrush="#7A5A1F" BorderThickness="1"
                Visibility="{Binding WorkDriveMounted, Converter={StaticResource InverseBoolToVis}}">
          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="Auto" />
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ui:SymbolIcon Grid.Column="0" Symbol="Warning24" FontSize="18" Margin="0,0,10,0"
                           VerticalAlignment="Center" Foreground="#F0C04A" />
            <TextBlock Grid.Column="1" VerticalAlignment="Center" TextWrapping="Wrap" Foreground="#F0D9A8"
                       Text="P: work drive is not mounted — building is disabled. Binarize resolves vanilla data and includes against P:. Mount it to build." />
            <ui:Button Grid.Column="2" Content="Mount P:" Appearance="Primary" VerticalAlignment="Center"
                       Margin="12,0,0,0" Command="{Binding MountWorkDriveCommand}" />
          </Grid>
        </Border>

        <!-- Projects list -->
        <ui:Card Margin="0,0,0,4" Padding="16">
          <StackPanel>
            <Grid Margin="0,0,0,12">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
              </Grid.ColumnDefinitions>
              <TextBlock Grid.Column="0" Text="Projects" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center" />
              <StackPanel Grid.Column="1" Orientation="Horizontal">
                <ui:TextBox Width="200" VerticalAlignment="Center" Margin="0,0,8,0"
                            PlaceholderText="filter projects…" Icon="{ui:SymbolIcon Search24}"
                            Text="{Binding ProjectFilter, UpdateSourceTrigger=PropertyChanged}" />
                <ui:Button Content="Add" Icon="{ui:SymbolIcon Add24}" Appearance="Primary"
                           ToolTip="Add a mod — new, import a folder, or clone from GitHub"
                           Margin="0,0,8,0" Click="OnAddMod" />
                <ui:Button Content="Dev tools" Icon="{ui:SymbolIcon Wrench24}" Margin="0,0,8,0"
                           ToolTip="Import APH Havoc Dev Tools — offline object editor, free camera and item spawner. Source is editable and rebuildable; enable the bundled client helper in a loadout to use it."
                           Visibility="{Binding DevToolsAvailable, Converter={StaticResource BoolToVis}}"
                           Command="{Binding ImportDevToolsCommand}" />
                <ui:Button Content="Refresh" Icon="{ui:SymbolIcon ArrowSync24}" Command="{Binding RefreshModProjectsCommand}" />
              </StackPanel>
            </Grid>
            <TextBlock x:Name="RowStatus" FontSize="12" TextWrapping="Wrap" Margin="0,0,0,8"
                       Visibility="Collapsed"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
            <ItemsControl ItemsSource="{Binding ModProjectsView}">
              <ItemsControl.ItemTemplate>
                <DataTemplate>
                 <Grid>
                  <!-- Standalone mod card (hidden for packs + broken links) -->
                  <Border Margin="0,0,0,8" Padding="12" CornerRadius="6"
                          Visibility="{Binding ShowStandalone, Converter={StaticResource BoolToVis}}"
                          Background="{DynamicResource ControlFillColorDefaultBrush}"
                          BorderBrush="{DynamicResource ControlElevationBorderBrush}" BorderThickness="1">
                    <Grid>
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                      </Grid.ColumnDefinitions>
                      <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal">
                          <TextBlock Text="{Binding Name}" FontWeight="SemiBold" VerticalAlignment="Center" />
                          <Border CornerRadius="9" Padding="7,1" Margin="8,0,0,0" VerticalAlignment="Center" Background="{Binding KindBg}">
                            <TextBlock Text="{Binding KindLabel}" FontSize="10" FontWeight="SemiBold" Foreground="{Binding KindFg}" />
                          </Border>
                        </StackPanel>
                        <TextBlock Text="{Binding Path}" FontSize="11" TextTrimming="CharacterEllipsis"
                                   Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                        <TextBlock FontSize="11" Margin="0,2,0,0"
                                   Foreground="{DynamicResource TextFillColorTertiaryBrush}">
                          <Run Text="git:" /><Run Text=" " /><Run Text="{Binding Git, Mode=OneWay}" />
                        </TextBlock>
                      </StackPanel>
                      <Border Grid.Column="1" CornerRadius="9" Padding="8,2" Margin="0,0,8,0" VerticalAlignment="Center">
                        <Border.Style>
                          <Style TargetType="Border">
                            <Setter Property="Background" Value="#5A1F1F" />
                            <Style.Triggers>
                              <DataTrigger Binding="{Binding Linked}" Value="True">
                                <Setter Property="Background" Value="#1F4D2E" />
                              </DataTrigger>
                            </Style.Triggers>
                          </Style>
                        </Border.Style>
                        <TextBlock FontSize="11" FontWeight="SemiBold">
                          <TextBlock.Style>
                            <Style TargetType="TextBlock">
                              <Setter Property="Text" Value="unlinked" />
                              <Setter Property="Foreground" Value="#FF6B6B" />
                              <Style.Triggers>
                                <DataTrigger Binding="{Binding Linked}" Value="True">
                                  <Setter Property="Text" Value="linked" />
                                  <Setter Property="Foreground" Value="#7CE3A1" />
                                </DataTrigger>
                              </Style.Triggers>
                            </Style>
                          </TextBlock.Style>
                        </TextBlock>
                      </Border>
                      <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                        <!-- Primary: Build. Everything else under the ⋯ menu. -->
                        <ui:Button Content="Build" Appearance="Primary" Icon="{ui:SymbolIcon Box24}"
                                   ToolTip="Build console — preflight findings, options, live log, diagnostics"
                                   Tag="{Binding Name}" Click="OnBuildMod" Margin="0,0,8,0" />
                        <ui:Button Icon="{ui:SymbolIcon BranchFork24}" Margin="0,0,8,0" Tag="{Binding Name}" Click="OnOpenGit"
                                   ToolTip="Git — branches, stage &amp; commit, push, release, terminal" />
                        <ui:Button Icon="{ui:SymbolIcon Open24}" Margin="0,0,8,0" Tag="{Binding RepoUrl}" Click="OnOpenRepoUrl"
                                   IsEnabled="{Binding HasRepoUrl}"
                                   ToolTip="Open the repo on GitHub (disabled when the project has no git remote)" />
                        <ui:DropDownButton Icon="{ui:SymbolIcon MoreHorizontal24}" ToolTip="More actions">
                          <ui:DropDownButton.Flyout>
                            <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                              <MenuItem Header="Open folder" Tag="{Binding Path}" Click="OnOpenModFolder">
                                <MenuItem.Icon><ui:SymbolIcon Symbol="FolderOpen24" /></MenuItem.Icon>
                              </MenuItem>
                              <MenuItem Header="Open in editor" Tag="{Binding Path}" Click="OnOpenInEditor">
                                <MenuItem.Icon><ui:SymbolIcon Symbol="Code24" /></MenuItem.Icon>
                              </MenuItem>
                              <Separator />
                              <MenuItem Header="Link (P:)" Tag="{Binding Name}" Click="OnQuickJunction">
                                <MenuItem.Icon><ui:SymbolIcon Symbol="Link24" /></MenuItem.Icon>
                                <MenuItem.Style>
                                  <Style TargetType="MenuItem">
                                    <Setter Property="Visibility" Value="Visible" />
                                    <Style.Triggers>
                                      <DataTrigger Binding="{Binding Linked}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                      </DataTrigger>
                                    </Style.Triggers>
                                  </Style>
                                </MenuItem.Style>
                              </MenuItem>
                              <MenuItem Header="Unlink (P:)" Tag="{Binding Name}" Click="OnUnlinkMod">
                                <MenuItem.Icon><ui:SymbolIcon Symbol="LinkDismiss24" /></MenuItem.Icon>
                                <MenuItem.Style>
                                  <Style TargetType="MenuItem">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                      <DataTrigger Binding="{Binding Linked}" Value="True">
                                        <Setter Property="Visibility" Value="Visible" />
                                      </DataTrigger>
                                    </Style.Triggers>
                                  </Style>
                                </MenuItem.Style>
                              </MenuItem>
                              <Separator />
                              <MenuItem Header="{Binding DeleteLabel}" Tag="{Binding Name}" Click="OnDeleteProject"
                                        Foreground="#FF6B6B">
                                <MenuItem.Icon><ui:SymbolIcon Symbol="Delete24" /></MenuItem.Icon>
                              </MenuItem>
                            </ContextMenu>
                          </ui:DropDownButton.Flyout>
                        </ui:DropDownButton>
                      </StackPanel>
                    </Grid>
                  </Border>

                  <!-- Pack group (shown only for packs): one repo, inner mods listed inside -->
                  <Expander Margin="0,0,0,8" IsExpanded="{Binding IsExpanded, Mode=TwoWay}"
                            Visibility="{Binding ShowPack, Converter={StaticResource BoolToVis}}"
                            Background="{DynamicResource ControlFillColorDefaultBrush}"
                            BorderBrush="{DynamicResource ControlElevationBorderBrush}" BorderThickness="1">
                    <Expander.Header>
                      <Grid Margin="4,6,8,6">
                        <Grid.ColumnDefinitions>
                          <ColumnDefinition Width="*" />
                          <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                          <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold" VerticalAlignment="Center" />
                            <Border CornerRadius="9" Padding="7,1" Margin="8,0,0,0" VerticalAlignment="Center"
                                    Background="{DynamicResource ControlFillColorSecondaryBrush}">
                              <TextBlock Text="{Binding PackSummary}" FontSize="10" FontWeight="SemiBold"
                                         Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                            </Border>
                          </StackPanel>
                          <TextBlock Text="{Binding Path}" FontSize="11" TextTrimming="CharacterEllipsis"
                                     Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                          <TextBlock FontSize="11" Margin="0,2,0,0" Foreground="{DynamicResource TextFillColorTertiaryBrush}">
                            <Run Text="git:" /><Run Text=" " /><Run Text="{Binding Git, Mode=OneWay}" />
                          </TextBlock>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,12,0">
                          <ui:Button Content="Build pack…" Appearance="Primary" Icon="{ui:SymbolIcon Box24}"
                                     Margin="0,0,8,0" Click="OnBuildPack"
                                     ToolTip="Build the inner mods into one @pack (Addons with a PBO per mod + keys)" />
                          <ui:Button Icon="{ui:SymbolIcon BranchFork24}" Margin="0,0,8,0" Tag="{Binding Name}" Click="OnOpenGit"
                                     ToolTip="Git — the whole pack is one repo" />
                          <ui:Button Icon="{ui:SymbolIcon Open24}" Margin="0,0,8,0" Tag="{Binding RepoUrl}" Click="OnOpenRepoUrl"
                                     IsEnabled="{Binding HasRepoUrl}"
                                     ToolTip="Open the repo on GitHub (disabled when the project has no git remote)" />
                          <!-- Same ⋯ menu as a standalone mod, scoped to the whole pack. -->
                          <ui:DropDownButton Icon="{ui:SymbolIcon MoreHorizontal24}" ToolTip="More actions">
                            <ui:DropDownButton.Flyout>
                              <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                                <MenuItem Header="Open folder" Tag="{Binding Path}" Click="OnOpenModFolder">
                                  <MenuItem.Icon><ui:SymbolIcon Symbol="FolderOpen24" /></MenuItem.Icon>
                                </MenuItem>
                                <MenuItem Header="Open in editor" Tag="{Binding Path}" Click="OnOpenInEditor">
                                  <MenuItem.Icon><ui:SymbolIcon Symbol="Code24" /></MenuItem.Icon>
                                </MenuItem>
                                <Separator />
                                <MenuItem Header="Link (P:)" Tag="{Binding Name}" Click="OnQuickJunction">
                                  <MenuItem.Icon><ui:SymbolIcon Symbol="Link24" /></MenuItem.Icon>
                                  <MenuItem.Style>
                                    <Style TargetType="MenuItem">
                                      <Setter Property="Visibility" Value="Visible" />
                                      <Style.Triggers>
                                        <DataTrigger Binding="{Binding Linked}" Value="True">
                                          <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                      </Style.Triggers>
                                    </Style>
                                  </MenuItem.Style>
                                </MenuItem>
                                <MenuItem Header="Unlink (P:)" Tag="{Binding Name}" Click="OnUnlinkMod">
                                  <MenuItem.Icon><ui:SymbolIcon Symbol="LinkDismiss24" /></MenuItem.Icon>
                                  <MenuItem.Style>
                                    <Style TargetType="MenuItem">
                                      <Setter Property="Visibility" Value="Collapsed" />
                                      <Style.Triggers>
                                        <DataTrigger Binding="{Binding Linked}" Value="True">
                                          <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                      </Style.Triggers>
                                    </Style>
                                  </MenuItem.Style>
                                </MenuItem>
                                <Separator />
                                <MenuItem Header="{Binding DeleteLabel}" Tag="{Binding Name}" Click="OnDeleteProject"
                                          Foreground="#FF6B6B">
                                  <MenuItem.Icon><ui:SymbolIcon Symbol="Delete24" /></MenuItem.Icon>
                                </MenuItem>
                              </ContextMenu>
                            </ui:DropDownButton.Flyout>
                          </ui:DropDownButton>
                        </StackPanel>
                      </Grid>
                    </Expander.Header>
                    <ItemsControl ItemsSource="{Binding Children}" Margin="24,6,0,4">
                      <ItemsControl.ItemTemplate>
                        <DataTemplate>
                          <Border Margin="0,0,0,6" Padding="8,6" CornerRadius="4"
                                  Background="{DynamicResource ControlFillColorDefaultBrush}"
                                  BorderBrush="{DynamicResource ControlElevationBorderBrush}" BorderThickness="1">
                            <Grid>
                              <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                              </Grid.ColumnDefinitions>
                              <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="13" />
                                <TextBlock Text="{Binding Path}" FontSize="11" TextTrimming="CharacterEllipsis"
                                           Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                              </StackPanel>
                              <ui:Button Grid.Column="1" Icon="{ui:SymbolIcon FolderOpen24}" Tag="{Binding Path}"
                                         Click="OnOpenModFolder" VerticalAlignment="Center" ToolTip="Open folder" />
                            </Grid>
                          </Border>
                        </DataTemplate>
                      </ItemsControl.ItemTemplate>
                    </ItemsControl>
                  </Expander>

                  <!-- Broken link: a leftover junction whose source folder is gone. Don't hide it — let the user
                       re-point or clear the dead links (mods\ + P:). Amber banner; actions stay clickable. -->
                  <Border Margin="0,0,0,8" Padding="12" CornerRadius="6"
                          Visibility="{Binding ShowBroken, Converter={StaticResource BoolToVis}}"
                          Background="#3A2A12" BorderBrush="#9A6A22" BorderThickness="1">
                    <Grid>
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                      </Grid.ColumnDefinitions>
                      <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal">
                          <ui:SymbolIcon Symbol="Warning24" FontSize="16" Foreground="#F0C04A" VerticalAlignment="Center" />
                          <TextBlock Text="{Binding Name}" FontWeight="SemiBold" Margin="8,0,0,0" VerticalAlignment="Center" />
                          <Border CornerRadius="9" Padding="7,1" Margin="8,0,0,0" VerticalAlignment="Center" Background="#5A3A12">
                            <TextBlock Text="broken link" FontSize="10" FontWeight="SemiBold" Foreground="#F0C04A" />
                          </Border>
                        </StackPanel>
                        <TextBlock Text="The source folder this links to is missing — the junction in mods\ (and on P:) points nowhere."
                                   FontSize="11" TextWrapping="Wrap" Margin="0,3,0,0"
                                   Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                      </StackPanel>
                      <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,4,0">
                        <ui:Button Content="Re-link…" Appearance="Primary" Icon="{ui:SymbolIcon Link24}"
                                   Margin="0,0,8,0" Tag="{Binding Name}" Click="OnRelinkProject"
                                   ToolTip="Point this mod at a new source folder (replaces the dead junction)" />
                        <ui:Button Content="Remove" Icon="{ui:SymbolIcon Delete24}" Tag="{Binding Name}" Click="OnRemoveBroken"
                                   ToolTip="Clear the leftover junctions (mods\ and P:). The source is already gone." />
                      </StackPanel>
                    </Grid>
                  </Border>
                 </Grid>
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>
            <TextBlock Text="(no mod projects yet — create or import one above)" FontSize="12"
                       Foreground="{DynamicResource TextFillColorTertiaryBrush}">
              <TextBlock.Style>
                <Style TargetType="TextBlock">
                  <Setter Property="Visibility" Value="Collapsed" />
                  <Style.Triggers>
                    <DataTrigger Binding="{Binding ModProjects.Count}" Value="0">
                      <Setter Property="Visibility" Value="Visible" />
                    </DataTrigger>
                  </Style.Triggers>
                </Style>
              </TextBlock.Style>
            </TextBlock>
          </StackPanel>
        </ui:Card>

      </StackPanel>
    </ScrollViewer>

    <!-- Blocking overlay: no projects root configured → the page can't do anything useful -->
    <Border Grid.Row="0" Grid.RowSpan="2" Background="#CC1A1A1A"
            Visibility="{Binding ModsBlocked, Converter={StaticResource BoolToVis}}">
      <Border CornerRadius="8" Padding="28" MaxWidth="440" VerticalAlignment="Center" HorizontalAlignment="Center"
              Background="{DynamicResource ApplicationBackgroundBrush}" BorderThickness="1"
              BorderBrush="{DynamicResource ControlElevationBorderBrush}">
        <StackPanel>
          <ui:SymbolIcon Symbol="Settings24" FontSize="34" HorizontalAlignment="Center" Foreground="#F0C04A" />
          <TextBlock Text="Mods aren't set up yet" FontSize="16" FontWeight="SemiBold"
                     HorizontalAlignment="Center" Margin="0,10,0,6" />
          <TextBlock Text="{Binding ModsBlockMessage}" TextWrapping="Wrap" TextAlignment="Center"
                     Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,0,16" />
          <ui:Button Content="Configure mods" Appearance="Primary" Icon="{ui:SymbolIcon Settings24}"
                     HorizontalAlignment="Center" Tag="mods" Click="OnModuleSettings" />
        </StackPanel>
      </Border>
    </Border>
  </Grid>
</UserControl>
