<UserControl x:Class="Dzl.Tray.Views.ToolsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:Dzl.Tray"
             xmlns:vm="clr-namespace:Dzl.Tray.ViewModels"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:MainViewModel}">
  <!-- Tools page (DayZ Tools catalog, work drive, Pack PBO / Batch PAA / Unbinarize).
       DataContext = MainViewModel (inherited from the host window). -->
  <UserControl.Resources>
    <!-- Small read-only mono output area for Tools (pack/paa/unbinarize results). -->
    <Style x:Key="ToolOutputBox" TargetType="TextBox">
      <Setter Property="IsReadOnly" Value="True" />
      <Setter Property="FontFamily" Value="Cascadia Mono, Consolas" />
      <Setter Property="FontSize" Value="12" />
      <Setter Property="TextWrapping" Value="NoWrap" />
      <Setter Property="AcceptsReturn" Value="True" />
      <Setter Property="Background" Value="{DynamicResource ApplicationBackgroundBrush}" />
      <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="BorderBrush" Value="{DynamicResource ControlElevationBorderBrush}" />
      <Setter Property="Padding" Value="8" />
      <Setter Property="VerticalScrollBarVisibility" Value="Auto" />
      <Setter Property="HorizontalScrollBarVisibility" Value="Auto" />
    </Style>
  </UserControl.Resources>

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <Grid Grid.Row="0" Margin="0,0,0,12">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>
      <TextBlock Grid.Column="0" Text="Tools" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center" />
      <ui:Button Grid.Column="1" Content="Refresh" Icon="{ui:SymbolIcon ArrowSync24}"
                 Click="OnRefreshTools" />
    </Grid>

    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
      <StackPanel Margin="0,0,14,0">

        <!-- DayZ Tools catalog -->
        <ui:Card Margin="0,0,0,12" Padding="16">
          <StackPanel>
            <TextBlock Text="DayZ Tools" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12" />
            <ItemsControl ItemsSource="{Binding Tools}">
              <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                  <WrapPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
              </ItemsControl.ItemsPanel>
              <ItemsControl.ItemTemplate>
                <DataTemplate>
                  <Button Width="190" Height="58" Margin="0,0,8,8" Padding="12,8"
                          HorizontalContentAlignment="Left"
                          IsEnabled="{Binding Exists}"
                          Opacity="{Binding Exists, Converter={StaticResource BoolToOpacity}}"
                          Command="{Binding DataContext.LaunchToolCommand, RelativeSource={RelativeSource AncestorType=ui:FluentWindow}}"
                          CommandParameter="{Binding}"
                          ToolTip="{Binding ExePath}">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                      <ui:SymbolIcon Symbol="Open24" FontSize="18" Margin="0,0,10,0" VerticalAlignment="Center" />
                      <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold"
                                   TextTrimming="CharacterEllipsis" />
                        <TextBlock Text="{Binding Kind}" FontSize="10"
                                   Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                      </StackPanel>
                    </StackPanel>
                  </Button>
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>
          </StackPanel>
        </ui:Card>

        <!-- Work drive -->
        <ui:Card Margin="0,0,0,12" Padding="16">
          <StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
              <TextBlock Text="Work drive" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center" />
              <local:InfoTip Text="{x:Static local:HelpText.WorkDrive}" />
            </StackPanel>
            <StackPanel Orientation="Horizontal">
              <Ellipse Width="10" Height="10" Margin="0,0,8,0" VerticalAlignment="Center"
                       Fill="{Binding WorkDriveMounted, Converter={StaticResource StatusDotConverter}}" />
              <TextBlock Text="{Binding WorkDriveStatus}" VerticalAlignment="Center" Margin="0,0,16,0" />
              <ui:Button Content="Mount" Appearance="Primary" Icon="{ui:SymbolIcon PlugConnected24}"
                         Margin="0,0,8,0" Command="{Binding MountWorkDriveCommand}" />
              <ui:Button Content="Unmount" Icon="{ui:SymbolIcon PlugDisconnected24}"
                         Margin="0,0,8,0" Command="{Binding UnmountWorkDriveCommand}" />
              <ui:Button Content="Refresh" Icon="{ui:SymbolIcon ArrowSync24}" Command="{Binding RefreshWorkDriveCommand}" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
              <ui:Button Content="Extract game data" Icon="{ui:SymbolIcon Box24}"
                         Margin="0,0,8,0" IsEnabled="{Binding CanExtractGame}"
                         Command="{Binding ExtractGameDataCommand}"
                         CommandParameter="{Binding IsChecked, ElementName=ForceExtractChk}"
                         ToolTip="Unpack every vanilla PBO to P: via BankRev (reliable, incremental). Replaces DayZ Tools' built-in extract." />
              <CheckBox x:Name="ForceExtractChk" Content="full re-extract" VerticalAlignment="Center" Margin="0,0,12,0"
                        ToolTip="Re-unpack every PBO, ignoring the incremental manifest." />
              <TextBlock Text="{Binding ExtractGameStatus}" VerticalAlignment="Center"
                         TextTrimming="CharacterEllipsis" MaxWidth="460" Opacity="0.85" />
            </StackPanel>
          </StackPanel>
        </ui:Card>

        <!-- Pack PBO -->
        <ui:Card Margin="0,0,0,12" Padding="16">
          <StackPanel>
            <TextBlock Text="Pack PBO" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12" />
            <Grid Margin="0,0,0,8">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
              </Grid.ColumnDefinitions>
              <ui:TextBox x:Name="PackSrcBox" Grid.Column="0" PlaceholderText="source folder…" />
              <ui:Button Grid.Column="1" Content="Browse" Icon="{ui:SymbolIcon FolderOpen24}"
                         Margin="8,0,0,0" Tag="packsrc" Click="OnBrowseFolderInto" />
            </Grid>
            <Grid Margin="0,0,0,8">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
              </Grid.ColumnDefinitions>
              <ui:TextBox x:Name="PackDstBox" Grid.Column="0" PlaceholderText="output folder…" />
              <ui:Button Grid.Column="1" Content="Browse" Icon="{ui:SymbolIcon FolderOpen24}"
                         Margin="8,0,0,0" Tag="packdst" Click="OnBrowseFolderInto" />
            </Grid>
            <ui:TextBox x:Name="PackPrefixBox" Margin="0,0,0,8" PlaceholderText="prefix (optional — defaults to $PBOPREFIX$ / folder name)" />
            <!-- Same options as a My Mods build: binarize, sign with a configured key, build-anyway. -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,8" VerticalAlignment="Center">
              <CheckBox x:Name="PackBinarizeChk" Content="binarize" IsChecked="True" VerticalAlignment="Center" Margin="0,0,16,0"
                        ToolTip="On: full build — binarize models + config.cpp→config.bin (needs P: mounted).&#10;Off: pack the folder as-is into a PBO." />
              <CheckBox x:Name="PackSignChk" Content="sign" VerticalAlignment="Center" Margin="0,0,8,0"
                        ToolTip="Sign the PBO with a configured key (Settings → Signing)." />
              <ComboBox x:Name="PackKeyCombo" MinWidth="150" FontSize="12" VerticalAlignment="Center" Margin="0,0,16,0"
                        IsEnabled="{Binding IsChecked, ElementName=PackSignChk}"
                        ToolTip="Signing key (from Settings → Signing)." />
              <CheckBox x:Name="PackBuildAnywayChk" Content="build anyway" VerticalAlignment="Center"
                        ToolTip="Run preflight but don't let its errors block packing (e.g. references to vanilla assets not on P:)." />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,8" VerticalAlignment="Center">
              <ui:Button x:Name="PreflightPackButton" Content="Preflight" Icon="{ui:SymbolIcon DocumentSearch24}"
                         Click="OnPreflightPack" Margin="0,0,8,0"
                         ToolTip="Check the folder for missing references / config issues before packing" />
              <ui:Button x:Name="PackButton" Content="Pack" Appearance="Primary"
                         Icon="{ui:SymbolIcon Box24}" Click="OnPackPbo" Margin="0,0,12,0" />
              <TextBlock x:Name="PackStatus" VerticalAlignment="Center" Opacity="0.85"
                         Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
            </StackPanel>
            <TextBox x:Name="PackOutput" Height="140" Style="{StaticResource ToolOutputBox}" />
          </StackPanel>
        </ui:Card>

        <!-- Batch PAA -->
        <ui:Card Margin="0,0,0,12" Padding="16">
          <StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
              <TextBlock Text="Batch PAA" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center" />
              <local:InfoTip Text="{x:Static local:HelpText.GameData}" />
            </StackPanel>
            <Grid Margin="0,0,0,8">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
              </Grid.ColumnDefinitions>
              <ui:TextBox x:Name="PaaDirBox" Grid.Column="0" PlaceholderText="image folder (png/tga)…" />
              <ui:Button Grid.Column="1" Content="Browse" Icon="{ui:SymbolIcon FolderOpen24}"
                         Margin="8,0,0,0" Tag="paadir" Click="OnBrowseFolderInto" />
            </Grid>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
              <CheckBox x:Name="PaaRecursive" Content="Recursive" VerticalAlignment="Center" Margin="0,0,16,0" />
              <ui:Button x:Name="PaaButton" Content="Convert" Appearance="Primary"
                         Icon="{ui:SymbolIcon Image24}" Click="OnConvertPaa" />
              <TextBlock x:Name="PaaToolMissing" Text="ImageToPAA not found"
                         VerticalAlignment="Center" Margin="12,0,0,0" Visibility="Collapsed"
                         Foreground="#FF6B6B" />
            </StackPanel>
            <TextBox x:Name="PaaOutput" Height="110" Style="{StaticResource ToolOutputBox}" />
          </StackPanel>
        </ui:Card>

        <!-- Unbinarize -->
        <ui:Card Margin="0,0,0,4" Padding="16">
          <StackPanel>
            <TextBlock Text="Unbinarize config.bin" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12" />
            <Grid Margin="0,0,0,8">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
              </Grid.ColumnDefinitions>
              <ui:TextBox x:Name="BinFileBox" Grid.Column="0" PlaceholderText="config.bin file…" />
              <ui:Button Grid.Column="1" Content="Browse" Icon="{ui:SymbolIcon DocumentSearch24}"
                         Margin="8,0,0,0" Click="OnBrowseBinFile" />
            </Grid>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
              <ui:Button x:Name="UnbinButton" Content="Unbinarize" Appearance="Primary"
                         Icon="{ui:SymbolIcon DocumentArrowDown24}" Click="OnUnbinarize" />
              <TextBlock x:Name="UnbinToolMissing" Text="CfgConvert not found"
                         VerticalAlignment="Center" Margin="12,0,0,0" Visibility="Collapsed"
                         Foreground="#FF6B6B" />
            </StackPanel>
            <TextBox x:Name="UnbinOutput" Height="90" Style="{StaticResource ToolOutputBox}" />
          </StackPanel>
        </ui:Card>

      </StackPanel>
    </ScrollViewer>
  </Grid>
</UserControl>
