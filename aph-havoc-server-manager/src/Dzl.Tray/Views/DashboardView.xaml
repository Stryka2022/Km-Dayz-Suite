<UserControl x:Class="Dzl.Tray.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:Dzl.Tray"
             xmlns:vm="clr-namespace:Dzl.Tray.ViewModels"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:MainViewModel}">
  <!-- Dashboard. DataContext = MainViewModel (inherited from the host window).
       Two target-scoped columns: LEFT = everything SERVER, RIGHT = everything CLIENT.
       Each column stacks control card -> launch command -> active mods -> edit shortcuts. -->
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />  <!-- summary strip -->
      <RowDefinition Height="*" />     <!-- two columns -->
    </Grid.RowDefinitions>

    <!-- Compact summary strip: mode / port / active profile -->
    <ui:Card Grid.Row="0" Margin="0,0,0,12" Padding="16,12">
      <StackPanel Orientation="Horizontal">
        <StackPanel Margin="0,0,32,0">
          <TextBlock Text="MODE" FontSize="11" FontWeight="SemiBold"
                     Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
          <TextBlock Text="{Binding Mode}" FontSize="18" FontWeight="SemiBold" />
        </StackPanel>
        <StackPanel Margin="0,0,32,0">
          <TextBlock Text="PORT" FontSize="11" FontWeight="SemiBold"
                     Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
          <TextBlock Text="{Binding Port}" FontSize="18" FontWeight="SemiBold" />
        </StackPanel>
        <StackPanel>
          <TextBlock Text="ACTIVE PROFILE" FontSize="11" FontWeight="SemiBold"
                     Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
          <TextBlock Text="{Binding ActivePreset}" FontSize="18" FontWeight="SemiBold" />
        </StackPanel>
      </StackPanel>
    </ui:Card>

    <!-- Two columns (server | client). Each column is a row-grid that fills the page
         height; the Active mods card takes the * row and scrolls internally. -->
    <Grid Grid.Row="1">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="16" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- ===== LEFT: SERVER ===== (whole column hidden on an offline sandbox) -->
        <Grid Grid.Column="0"
              Visibility="{Binding IsOnlineInstance, Converter={StaticResource BoolToVis}}">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />  <!-- control card -->
            <RowDefinition Height="Auto" />  <!-- launch command -->
            <RowDefinition Height="Auto" />  <!-- mission source -->
            <RowDefinition Height="*" />     <!-- active mods (grows) -->
            <RowDefinition Height="Auto" />  <!-- shortcuts -->
          </Grid.RowDefinitions>
          <!-- Control card (hidden entirely on an offline sandbox — there is no server) -->
          <ui:Card Grid.Row="0" Margin="0,0,0,12" Padding="16"
                   Visibility="{Binding IsOnlineInstance, Converter={StaticResource BoolToVis}}">
            <StackPanel>
              <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                <ui:SymbolIcon Symbol="Server24" FontSize="20" Margin="0,0,8,0" VerticalAlignment="Center" />
                <TextBlock Text="Server" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center" />
                <Border CornerRadius="10" Padding="8,2" Margin="12,0,0,0" VerticalAlignment="Center"
                        Background="{DynamicResource ControlFillColorDefaultBrush}">
                  <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Ellipse Width="9" Height="9" Margin="0,0,6,0" VerticalAlignment="Center"
                             Fill="{Binding ServerUp, Converter={StaticResource StatusDotConverter}}" />
                    <TextBlock Text="{Binding ServerStatus}" FontSize="11" VerticalAlignment="Center"
                               Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                  </StackPanel>
                </Border>
              </StackPanel>
              <StackPanel Orientation="Horizontal">
                <ui:Button Content="Start" Appearance="Success" Icon="{ui:SymbolIcon Play24}"
                           MinWidth="90" Margin="0,0,8,0" Command="{Binding StartServerCommand}" />
                <ui:Button Content="Stop" Appearance="Danger" Icon="{ui:SymbolIcon Stop24}"
                           MinWidth="90" Margin="0,0,8,0" Command="{Binding StopServerCommand}" />
                <ui:Button Content="Restart" Icon="{ui:SymbolIcon ArrowClockwise24}"
                           MinWidth="90" Command="{Binding RestartServerCommand}" />
              </StackPanel>
            </StackPanel>
          </ui:Card>

          <!-- Launch command card -->
          <ui:Card Grid.Row="1" Margin="0,0,0,12" Padding="16">
            <StackPanel>
              <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                <TextBlock Text="Launch command" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center" />
                <local:InfoTip Text="{x:Static local:HelpText.Mode}" />
              </StackPanel>
              <Border CornerRadius="4" Padding="10"
                      Background="{DynamicResource ControlFillColorDefaultBrush}">
                <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="140">
                  <TextBox Text="{Binding ServerArgv, Mode=OneWay}" IsReadOnly="True"
                           BorderThickness="0" Background="Transparent"
                           FontFamily="Cascadia Mono, Consolas" FontSize="12"
                           TextWrapping="Wrap" />
                </ScrollViewer>
              </Border>
            </StackPanel>
          </ui:Card>

          <!-- Mission source: which mpmissions the SERVER actually loads (serverDZ.cfg template vs the
               engine's $currentdir = install). Green = this instance, amber = the install (Fix available). -->
          <ui:Card Grid.Row="2" Margin="0,0,0,12" Padding="16">
            <StackPanel>
              <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                <TextBlock Text="Mission source" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center" />
                <local:InfoTip Text="{x:Static local:HelpText.MissionSource}" />
                <Border CornerRadius="10" Padding="8,2" Margin="12,0,0,0" VerticalAlignment="Center"
                        Background="{DynamicResource ControlFillColorDefaultBrush}">
                  <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Ellipse Width="9" Height="9" Margin="0,0,6,0" VerticalAlignment="Center">
                      <Ellipse.Style>
                        <Style TargetType="Ellipse">
                          <Setter Property="Fill" Value="{StaticResource StatusDownBrush}" />
                          <Style.Triggers>
                            <DataTrigger Binding="{Binding MissionKind}" Value="instance">
                              <Setter Property="Fill" Value="{StaticResource MissionOkBrush}" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding MissionKind}" Value="install">
                              <Setter Property="Fill" Value="{StaticResource MissionWarnBrush}" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding MissionKind}" Value="missing">
                              <Setter Property="Fill" Value="{StaticResource MissionErrorBrush}" />
                            </DataTrigger>
                          </Style.Triggers>
                        </Style>
                      </Ellipse.Style>
                    </Ellipse>
                    <TextBlock Text="{Binding MissionStatusLabel}" FontSize="11" VerticalAlignment="Center"
                               Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                  </StackPanel>
                </Border>
              </StackPanel>
              <Border CornerRadius="4" Padding="10"
                      Background="{DynamicResource ControlFillColorDefaultBrush}">
                <TextBox Text="{Binding MissionEffectivePath, Mode=OneWay}" IsReadOnly="True"
                         BorderThickness="0" Background="Transparent"
                         FontFamily="Cascadia Mono, Consolas" FontSize="12" TextWrapping="Wrap" />
              </Border>
              <TextBlock Text="{Binding MissionMessage}" FontSize="11" Margin="0,6,0,0"
                         Foreground="{DynamicResource TextFillColorTertiaryBrush}" TextWrapping="Wrap" />
              <ui:Button Content="Fix — point at this instance's mission" Appearance="Caution"
                         Margin="0,10,0,0"
                         Visibility="{Binding MissionFixVisible, Converter={StaticResource BoolToVis}}"
                         Command="{Binding FixMissionTemplateCommand}" />
            </StackPanel>
          </ui:Card>

          <!-- Active mods (server). Border fills the * row (Card sizes to content). -->
          <Border Grid.Row="3" Margin="0,0,0,12" Padding="16" CornerRadius="8"
                  Background="{DynamicResource ControlFillColorDefaultBrush}"
                  BorderBrush="{DynamicResource ControlElevationBorderBrush}" BorderThickness="1">
            <Grid>
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
              </Grid.RowDefinitions>
              <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                <TextBlock Text="Active mods" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center" />
                <TextBlock Text="{Binding ServerModsCount, StringFormat=' ({0})'}" FontSize="14"
                           VerticalAlignment="Center"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                <local:InfoTip Text="{x:Static local:HelpText.ModSide}" />
              </StackPanel>
              <Border Grid.Row="1" CornerRadius="4" Padding="4"
                      Background="{DynamicResource ControlFillColorDefaultBrush}">
                <Grid>
                  <TextBlock Text="(none enabled)" Margin="8,6" FontSize="12"
                             Foreground="{DynamicResource TextFillColorTertiaryBrush}">
                    <TextBlock.Style>
                      <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                          <DataTrigger Binding="{Binding ServerModsCount}" Value="0">
                            <Setter Property="Visibility" Value="Visible" />
                          </DataTrigger>
                        </Style.Triggers>
                      </Style>
                    </TextBlock.Style>
                  </TextBlock>
                  <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding ServerMods}">
                      <ItemsControl.ItemTemplate>
                        <DataTemplate>
                          <StackPanel Orientation="Horizontal" Margin="8,3">
                            <TextBlock Text="{Binding Label}" FontSize="13" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                            <Border CornerRadius="9" Padding="6,1" Margin="8,0,0,0" VerticalAlignment="Center" Background="{Binding KindBg}">
                              <TextBlock Text="{Binding KindLabel}" FontSize="9" FontWeight="SemiBold" Foreground="{Binding KindFg}" />
                            </Border>
                          </StackPanel>
                        </DataTemplate>
                      </ItemsControl.ItemTemplate>
                    </ItemsControl>
                  </ScrollViewer>
                </Grid>
              </Border>
            </Grid>
          </Border>

          <!-- Shortcuts -->
          <StackPanel Grid.Row="4" Orientation="Horizontal">
            <ui:Button Content="Edit mods" Icon="{ui:SymbolIcon Apps24}"
                       Margin="0,0,8,0" Click="OnEditMods" />
            <ui:Button Content="Edit params" Icon="{ui:SymbolIcon Settings24}"
                       Tag="server" Click="OnEditParams" />
          </StackPanel>
        </Grid>

        <!-- ===== RIGHT: CLIENT ===== -->
        <Grid>
          <!-- Client column: spans the full page on an offline sandbox (the server column is
               hidden). Grid.Column set via style, not inline — a local value would beat the trigger. -->
          <Grid.Style>
            <Style TargetType="Grid">
              <Setter Property="Grid.Column" Value="2" />
              <Setter Property="Grid.ColumnSpan" Value="1" />
              <Style.Triggers>
                <DataTrigger Binding="{Binding IsOfflineInstance}" Value="True">
                  <Setter Property="Grid.Column" Value="0" />
                  <Setter Property="Grid.ColumnSpan" Value="3" />
                </DataTrigger>
              </Style.Triggers>
            </Style>
          </Grid.Style>
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />  <!-- control card -->
            <RowDefinition Height="Auto" />  <!-- offline mission card -->
            <RowDefinition Height="Auto" />  <!-- launch command -->
            <RowDefinition Height="*" />     <!-- active mods (grows) -->
            <RowDefinition Height="Auto" />  <!-- shortcuts -->
          </Grid.RowDefinitions>
          <!-- Control card -->
          <ui:Card Grid.Row="0" Margin="0,0,0,12" Padding="16">
            <StackPanel>
              <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                <ui:SymbolIcon Symbol="Desktop24" FontSize="20" Margin="0,0,8,0" VerticalAlignment="Center" />
                <TextBlock Text="Client" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center" />
                <Border CornerRadius="10" Padding="8,2" Margin="12,0,0,0" VerticalAlignment="Center"
                        Background="{DynamicResource ControlFillColorDefaultBrush}">
                  <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Ellipse Width="9" Height="9" Margin="0,0,6,0" VerticalAlignment="Center"
                             Fill="{Binding ClientUp, Converter={StaticResource StatusDotConverter}}" />
                    <TextBlock Text="{Binding ClientStatus}" FontSize="11" VerticalAlignment="Center"
                               Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                  </StackPanel>
                </Border>
              </StackPanel>
              <StackPanel Orientation="Horizontal">
                <!-- Normal instance: connect-to-server vs boot-offline are two explicit choices. -->
                <ui:Button Content="Start (online)" Appearance="Success" Icon="{ui:SymbolIcon Play24}"
                           MinWidth="90" Margin="0,0,8,0"
                           ToolTip="Connect the client to the running server"
                           Visibility="{Binding IsOnlineInstance, Converter={StaticResource BoolToVis}}"
                           Command="{Binding StartClientCommand}" />
                <ui:Button Content="Start (offline)" Appearance="Success" Icon="{ui:SymbolIcon Play24}"
                           MinWidth="90" Margin="0,0,8,0"
                           ToolTip="Load mods + mission without -connect — boots the mission offline (debug) / the main menu (normal)"
                           Visibility="{Binding IsOnlineInstance, Converter={StaticResource BoolToVis}}"
                           Command="{Binding StartClientNoConnectCommand}" />
                <!-- Offline instance: no server exists, so Start is always offline. -->
                <ui:Button Content="Start" Appearance="Success" Icon="{ui:SymbolIcon Play24}"
                           MinWidth="90" Margin="0,0,8,0"
                           Visibility="{Binding IsOfflineInstance, Converter={StaticResource BoolToVis}}"
                           Command="{Binding StartClientCommand}" />
                <ui:Button Content="Stop" Appearance="Danger" Icon="{ui:SymbolIcon Stop24}"
                           MinWidth="90" Margin="0,0,8,0" Command="{Binding StopClientCommand}" />
                <ui:Button Content="Restart" Icon="{ui:SymbolIcon ArrowClockwise24}"
                           MinWidth="90" Command="{Binding RestartClientCommand}" />
              </StackPanel>
            </StackPanel>
          </ui:Card>

          <!-- Offline mission card (offline instances only): is init.c patched to spawn a character? -->
          <ui:Card Grid.Row="1" Margin="0,0,0,12" Padding="16"
                   Visibility="{Binding OfflineInitCardVisible, Converter={StaticResource BoolToVis}}">
            <StackPanel>
              <Grid Margin="0,0,0,8">
                <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                  <TextBlock Text="Offline mission" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center" />
                  <Border CornerRadius="10" Padding="8,2" Margin="10,0,0,0" VerticalAlignment="Center"
                          Background="{DynamicResource ControlFillColorDefaultBrush}">
                    <StackPanel Orientation="Horizontal">
                      <Ellipse Width="9" Height="9" Margin="0,0,6,0" VerticalAlignment="Center">
                        <Ellipse.Style>
                          <Style TargetType="Ellipse">
                            <Setter Property="Fill" Value="{StaticResource StatusDownBrush}" />
                            <Style.Triggers>
                              <DataTrigger Binding="{Binding OfflineInitKind}" Value="patched">
                                <Setter Property="Fill" Value="{StaticResource MissionOkBrush}" />
                              </DataTrigger>
                              <DataTrigger Binding="{Binding OfflineInitKind}" Value="needspatch">
                                <Setter Property="Fill" Value="{StaticResource MissionWarnBrush}" />
                              </DataTrigger>
                            </Style.Triggers>
                          </Style>
                        </Ellipse.Style>
                      </Ellipse>
                      <TextBlock Text="{Binding OfflineInitStatusLabel}" FontSize="11" VerticalAlignment="Center"
                                 Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                    </StackPanel>
                  </Border>
                </StackPanel>
                <ui:Button Grid.Column="1" Icon="{ui:SymbolIcon Copy24}" Appearance="Secondary" Padding="8,4"
                           ToolTip="Copy the offline-bootstrap code (hand-edit fallback)"
                           Command="{Binding CopyOfflineInitSnippetCommand}" />
              </Grid>
              <TextBlock Text="{Binding OfflineInitMessage}" FontSize="11" TextWrapping="Wrap"
                         Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
              <ui:Button Content="Patch init.c — spawn into the world offline" Appearance="Caution"
                         Margin="0,10,0,0"
                         Visibility="{Binding OfflineInitFixVisible, Converter={StaticResource BoolToVis}}"
                         Command="{Binding PatchOfflineInitCommand}" />
            </StackPanel>
          </ui:Card>

          <!-- Launch command card -->
          <ui:Card Grid.Row="2" Margin="0,0,0,12" Padding="16">
            <StackPanel>
              <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                <TextBlock Text="Launch command" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center" />
                <local:InfoTip Text="{x:Static local:HelpText.Mode}" />
              </StackPanel>
              <Border CornerRadius="4" Padding="10"
                      Background="{DynamicResource ControlFillColorDefaultBrush}">
                <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="140">
                  <TextBox Text="{Binding ClientArgv, Mode=OneWay}" IsReadOnly="True"
                           BorderThickness="0" Background="Transparent"
                           FontFamily="Cascadia Mono, Consolas" FontSize="12"
                           TextWrapping="Wrap" />
                </ScrollViewer>
              </Border>
            </StackPanel>
          </ui:Card>

          <!-- Active mods (client). Border fills the * row (Card sizes to content). -->
          <Border Grid.Row="3" Margin="0,0,0,12" Padding="16" CornerRadius="8"
                  Background="{DynamicResource ControlFillColorDefaultBrush}"
                  BorderBrush="{DynamicResource ControlElevationBorderBrush}" BorderThickness="1">
            <Grid>
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
              </Grid.RowDefinitions>
              <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                <TextBlock Text="Active mods" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center" />
                <TextBlock Text="{Binding ClientModsCount, StringFormat=' ({0})'}" FontSize="14"
                           VerticalAlignment="Center"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                <local:InfoTip Text="{x:Static local:HelpText.ModSide}" />
              </StackPanel>
              <Border Grid.Row="1" CornerRadius="4" Padding="4"
                      Background="{DynamicResource ControlFillColorDefaultBrush}">
                <Grid>
                  <TextBlock Text="(none enabled)" Margin="8,6" FontSize="12"
                             Foreground="{DynamicResource TextFillColorTertiaryBrush}">
                    <TextBlock.Style>
                      <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                          <DataTrigger Binding="{Binding ClientModsCount}" Value="0">
                            <Setter Property="Visibility" Value="Visible" />
                          </DataTrigger>
                        </Style.Triggers>
                      </Style>
                    </TextBlock.Style>
                  </TextBlock>
                  <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding ClientMods}">
                      <ItemsControl.ItemTemplate>
                        <DataTemplate>
                          <StackPanel Orientation="Horizontal" Margin="8,3">
                            <TextBlock Text="{Binding Label}" FontSize="13" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                            <Border CornerRadius="9" Padding="6,1" Margin="8,0,0,0" VerticalAlignment="Center" Background="{Binding KindBg}">
                              <TextBlock Text="{Binding KindLabel}" FontSize="9" FontWeight="SemiBold" Foreground="{Binding KindFg}" />
                            </Border>
                          </StackPanel>
                        </DataTemplate>
                      </ItemsControl.ItemTemplate>
                    </ItemsControl>
                  </ScrollViewer>
                </Grid>
              </Border>
            </Grid>
          </Border>

          <!-- Shortcuts -->
          <StackPanel Grid.Row="4" Orientation="Horizontal">
            <ui:Button Content="Edit mods" Icon="{ui:SymbolIcon Apps24}"
                       Margin="0,0,8,0" Click="OnEditMods" />
            <ui:Button Content="Edit params" Icon="{ui:SymbolIcon Settings24}"
                       Tag="client" Click="OnEditParams" />
          </StackPanel>
        </Grid>
      </Grid>
  </Grid>
</UserControl>
