<ui:FluentWindow x:Class="Dzl.Tray.EconomyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:ctl="clr-namespace:Dzl.Tray.Controls"
        Title="APH Havoc Server Manager — Economy" Width="1280" Height="820"
        MinWidth="860" MinHeight="560"
        WindowBackdropType="Mica" ExtendsContentIntoTitleBar="True"
        ShowInTaskbar="True" WindowStartupLocation="CenterScreen">

  <Grid Margin="16,8,16,16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <ui:TitleBar Grid.Row="0" Title="APH Havoc Server Manager — Economy" Height="30" ShowMaximize="True" ShowMinimize="True" Margin="0,0,0,8">
      <ui:TitleBar.TrailingContent>
        <ctl:ScreenshotButton VerticalAlignment="Stretch" />
      </ui:TitleBar.TrailingContent>
      <ui:TitleBar.Icon>
        <ui:SymbolIcon Symbol="Money24" />
      </ui:TitleBar.Icon>
    </ui:TitleBar>

    <ctl:EconomyPanel Grid.Row="1" />
  </Grid>
</ui:FluentWindow>
