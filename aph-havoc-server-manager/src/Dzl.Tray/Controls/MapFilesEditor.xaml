<UserControl x:Class="Dzl.Tray.Controls.MapFilesEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:ctl="clr-namespace:Dzl.Tray.Controls"
             xmlns:help="clr-namespace:Dzl.Tray.Help"
             x:Name="Root">
  <!-- DataContext = MapFilesVm. Auto-generated terrain data (mapgroup*/mapcluster*) is exported in-game, not
       hand-edited — this surface only locates + opens the files externally (VS Code / reveal / mission folder).
       A UserControl does NOT inherit host-window resources — local styles only; DynamicResource theme brushes ok. -->
  <UserControl.Resources>
    <Style x:Key="MfHint" TargetType="TextBlock">
      <Setter Property="FontSize" Value="11" />
      <Setter Property="Foreground" Value="{DynamicResource TextFillColorTertiaryBrush}" />
      <Setter Property="TextWrapping" Value="Wrap" />
    </Style>
  </UserControl.Resources>

  <Grid Margin="0,12,0,0">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />  <!-- toolbar -->
      <RowDefinition Height="*" />     <!-- list -->
    </Grid.RowDefinitions>

    <Grid Grid.Row="0" Margin="0,0,0,8">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>
      <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
        <ui:SymbolIcon Symbol="FolderOpen24" Margin="0,0,6,0" Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
        <TextBlock Text="{Binding MissionDir}" VerticalAlignment="Center" FontSize="12"
                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                   TextTrimming="CharacterEllipsis" MaxWidth="520" />
      </StackPanel>
      <StackPanel Grid.Column="1" Orientation="Horizontal">
        <ui:Button Content="Open mission folder" Icon="{ui:SymbolIcon FolderOpen24}" Margin="0,0,6,0"
                   IsEnabled="{Binding HasMission}" Click="OnOpenFolderClick" />
        <ctl:GlossaryButton Title="Map files" Entries="{x:Static help:Glossary.MapFiles}" Margin="0,0,12,0" />
        <ui:Button Content="Reload" Click="OnReloadClick" ToolTip="Re-scan the mission for map-data files" />
      </StackPanel>
    </Grid>

    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
      <StackPanel>
        <TextBlock Style="{StaticResource MfHint}" Margin="2,0,0,10"
                   Text="Auto-generated terrain data (building / cluster spawn positions). These are exported in-game, not edited by hand — open them in an external editor when you need to inspect or tweak raw coordinates." />

        <!-- Empty state -->
        <Border Visibility="{Binding HasFiles, Converter={StaticResource InverseBoolToVis}}"
                CornerRadius="8" Padding="14" Background="{DynamicResource ControlFillColorSecondaryBrush}"
                BorderBrush="{DynamicResource ControlElevationBorderBrush}" BorderThickness="1">
          <TextBlock Style="{StaticResource MfHint}"
                     Text="No map-data files found in this mission (or no mission resolved). They appear after an in-game export." />
        </Border>

        <ItemsControl ItemsSource="{Binding Files}">
          <ItemsControl.ItemTemplate>
            <DataTemplate>
              <Border CornerRadius="8" Padding="12,8" Margin="0,0,0,6"
                      Background="{DynamicResource ControlFillColorDefaultBrush}"
                      BorderBrush="{DynamicResource ControlElevationBorderBrush}" BorderThickness="1">
                <Grid>
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" />
                  </Grid.ColumnDefinitions>
                  <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal">
                      <ui:SymbolIcon Symbol="Document24" FontSize="15" Margin="0,0,6,0"
                                     Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                      <TextBlock Text="{Binding Name}" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center"
                                 Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                      <TextBlock Text="{Binding Size}" Margin="8,0,0,0" VerticalAlignment="Center"
                                 FontSize="11" Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                    </StackPanel>
                    <TextBlock Text="{Binding Description}" Style="{StaticResource MfHint}" Margin="21,2,0,0" />
                  </StackPanel>
                  <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <ui:Button Content="VS Code" FontSize="12" Padding="8,3" Margin="0,0,6,0"
                               Click="OnEditClick" Tag="{Binding}"
                               ToolTip="Open in VS Code (or the OS default editor for .xml)" />
                    <ui:Button Content="Reveal" Icon="{ui:SymbolIcon FolderOpen24}" FontSize="12" Padding="8,3"
                               Click="OnRevealClick" Tag="{Binding}" ToolTip="Reveal in Explorer" />
                  </StackPanel>
                </Grid>
              </Border>
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>
      </StackPanel>
    </ScrollViewer>
  </Grid>
</UserControl>
